{
  "name": "baptism-invite-app",
  "private": true,
  "version": "0.1.0",
  "description": "Baptism invitation web app — built on @cartography-lab/* design system",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "tokens:build": "npm run tokens:build --prefix ../cartography-lab-app",
    "dev": "node scripts/dev.mjs",
    "start": "npm run dev",
    "build": "npm run tokens:build && npm run build -w baptism-invite-web",
    "preview": "npm run preview -w baptism-invite-web",
    "typecheck": "npm run typecheck -w baptism-invite-web"
  },
  "engines": {
    "node": ">=20"
  }
}
